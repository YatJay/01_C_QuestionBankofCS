/*从命令行输入源文件名（含路径）和目标文件名（含路径），实现将源文件复制到目标文件。
注意事项：
	1. 源文件可能是文件文件，也可能是二进制文件。
	2. 源文件名和目标文件名只从命令行输入，当命令行格式不正确（参数个数不为 3）时，应报错。
	3. 程序的返回值（即由 main() 函数 return 的值和程序使用 exit() 终止运行时返回的值，也称退出代码）
规定为：
	a) 复制成功，返回 0。
	b) 命令行格式不对，返回 1。
	c) 源文件打开失败，返回 2。
	d) 目标文件创建失败，返回 3
	e) 向目标文件写数据的过程中出错，返回 4。
4. 向目标文件写数据的过程中出错的情况很少发生， 用户根据图例中的输入数据进行测试时，很可能不会出错，但程序应考虑出错的情况
（例如磁盘空间不够、往 U 盘上写一个大文件的过程中 U 盘出错或被拔走）。*/

#include <stdio.h>
int main(void){
    
    
    return 0;
}
